kind: ZarfPackageConfig
metadata:
  name: upgrade-test
  architecture: arm64

components:
  - name: upgrade-test
    required: true
    namespace: podinfo
    manifests:
      - name: podinfo
        namespace: podinfo
        files:
          - podinfo/podinfo-install.yaml
